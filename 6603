#include <iostream>
#include <cstdio>

using namespace std;
int arr[50];
int main(void) {
	int num = 1;
	while (num != 0)
	{
		cin >> num;
		for (int i = 0; i < num; i++)
		{
			cin >> arr[i];
		}
	}
	return 0;
}
