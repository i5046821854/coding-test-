#include <iostream>
#include <cstdio>
#include <algorithm>
#include <stack>
#include <queue>

using namespace std;
int arr[401][401];

int main(void)
{
	int v, e;
	cin >> v >> e;
	int a, b, c;
	for (int i = 0; i < e; i++)
	{
		cin >> a >> b >> c;
		arr[a][b] = c;
	}
	for (int i = 0; i < v; i++)
	{
		
	}
}
